import { blogImg } from '../../../helpers/blog-img';
import { module, test } from 'qunit';

module('Unit | Helper | blog img');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = blogImg(42);
  assert.ok(result);
});
