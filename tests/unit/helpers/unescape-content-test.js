import { unescapeContent } from '../../../helpers/unescape-content';
import { module, test } from 'qunit';

module('Unit | Helper | unescape content');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = unescapeContent(42);
  assert.ok(result);
});
